Meals for the week of: 
<div class="button-log">
<%= link_to @meal_plans[:this_week].weekof, meal_plan_path(@meal_plans[:this_week].id), class: "button button-show button-log-radius" %>

<% if @meal_plans[:next_week] != nil %>
<%= link_to @meal_plans[:next_week].weekof, meal_plan_path(@meal_plans[:next_week].id), class: "button button-show button-log-radius" %>
<% end %>
</div>
<%= render "/shared/meals_layout", meals: @meal_plan.dinners %>
